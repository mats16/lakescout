resources:
  apps:
    lakebrownie_app:
      name: ${var.app_name}
      description: Log exploration on Databricks
      source_code_path: ../
      #resources:
      #  - name: database-url-secret
      #    description: Database connection string
      #    secret:
      #      scope: lakebrownie-${bundle.target}
      #      key: database-url
      #      permission: READ
      user_api_scopes:
        - mcp.genie
        - mcp.vectorsearch
        - mcp.functions
        - mcp.external
        - sql
        - dashboards.genie
        - files.files
        - serving.serving-endpoints
        - catalog.catalogs:read
        - catalog.schemas:read
        - catalog.tables:read
        - workspace.workspace
      permissions:
        - level: CAN_USE
          group_name: users
